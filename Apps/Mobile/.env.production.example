# Copy to .env before release/EAS builds (file is gitignored).
# Use origin.* for mobile: full API (PUT/POST) via Next proxy → Flora.API. social.* CDN blocks PUT.
EXPO_PUBLIC_API_URL=https://origin.flora-s.net
# EXPO_PUBLIC_SENTRY_DSN=
