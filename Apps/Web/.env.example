# Copy to .env.local (development) or .env.production.local (production) and fill in.
# These files are gitignored — never commit real values.

# Base URL of the Flora.API backend the web app proxies to.
FLORA_API_UPSTREAM=http://127.0.0.1:5284

# Production only: set NODE_ENV=production in .env.production.local.
# NODE_ENV=production
