# Flora — эталон цветоподбора

Документ фиксирует **условия просмотра**, на которых подбиралась UI-палитра Flora (web как первичный эталон), и **признанные брендовые цвета** — `bg`, `whiteTemplate`, `greenLight`, `greenDark` в sRGB hex. Бренд в коде — абстрактные hex, а не «как нарисовал конкретный монитор»; монитор ниже — рабочий инструмент подбора, не сертифицированный color reference.

Зафиксировано: **2026-07-04**.

---

## Первичный эталон (web)

### Монитор

| Параметр | Значение |
|----------|----------|
| Модель | **Philips 272E1GJ** (PHL 272E1GJ) |
| Тип панели | VA LCD, 27", Full HD |
| Разрешение / частота | **1920 × 1080**, **60 Hz** |
| Подключение | NVIDIA GeForce RTX 4060 Ti |
| Variable refresh rate | Не поддерживается |
| Глубина цвета | **8-bit** |
| Формат сигнала | **RGB** |
| Диапазон | **SDR** (стандартный динамический диапазон) |

Источник параметров: Windows → Параметры → Система → Дисплей → Дополнительные параметры дисплея → **Сведения о дисплее** (скрин от 2026-07-04).

### Окружение при подборе цветов

| Параметр | Рекомендация |
|----------|--------------|
| Клиент | **Apps/Web** в браузере (Chrome / Edge), без расширений, меняющих цвета |
| Тема ОС | Тёмная |
| HDR Windows | **Выключен** для этого монитора (SDR) |
| Цветовой профиль | sRGB / SDR — не переключать между сессиями подбора |
| Яркость монитора | Одно стабильное значение на время правок (зафиксировать вручную, если меняли) |

> **Ограничение:** Philips 272E1GJ — потребительский SDR-монитор без заводской color-critical калибровки. Достаточен для day-to-day UI и выбора оттенков; для print/Pantone или юридически точного брендбука нужен отдельный процесс (калибратор, профили).

---

## Вторичная сверка (mobile)

| Параметр | Значение |
|----------|----------|
| Устройство | **Samsung Galaxy S25** |
| Режим экрана при проверке | **Natural** (не Vivid) |
| Ожидание | Тот же hex выглядит контрастнее и насыщеннее (AMOLED, широкий gamut); mobile не подгоняется под OLED отдельными «брендовыми» hex — используются те же токены + приглушённые mix, как на web |

---

## Канон палитры в репозитории

| Слой | Файл | Роль |
|------|------|------|
| Web CSS variables | `Apps/Web/app/globals.css` (`:root`) | Источник базовых `--flora-*` и части производных hex |
| Кросс-клиент | `Packages/flora-client-core/src/display/floraThemeTokens.ts` | Канон hex для mobile/desktop; предвычисленные `color-mix` для React Native |
| Mobile обёртка | `Apps/Mobile/lib/theme.ts` | `floraColors`, spacing, доменные константы чата/ленты |

**Правило изменений:** править базовые оттенки в `globals.css` → синхронизировать `FLORA_THEME_TOKENS` → при необходимости обновить предвычисленные mix и тест в `Packages/flora-client-core/src/display/display.test.ts`.

---

## Признанные брендовые цвета (sRGB)

Официальная палитра Flora — **четыре** базовых оттенка. Их hex — канон бренда в digital; менять только осознанно, с обновлением этого документа и синхронизацией `globals.css` + `FLORA_THEME_TOKENS`.

| Бренд-токен | CSS (web) | Hex | Роль в идентичности |
|-------------|-----------|-----|---------------------|
| **bg** | `--flora-bg` | `#0c0c0c` | Основной фон, «ночная» база интерфейса |
| **whiteTemplate** | `--flora-white-template` | `#fafafa` | Светлый текст и светлые UI-элементы на тёмном фоне |
| **greenLight** | `--flora-green-light` | `#a4d18a` | Главный акцент бренда (ссылки, активные состояния, ключевые акценты) |
| **greenDark** | `--flora-green-dark` | `#2c3527` | Вторичный зелёный бренда (тёмные заливки, контраст к `greenLight`) |

Ключи в коде: `FLORA_THEME_TOKENS.bg`, `.whiteTemplate`, `.greenLight`, `.greenDark` и одноимённые CSS-переменные.

---

## Служебные цвета UI (не брендовые)

Используются в продукте, но **не входят** в официальную четвёрку бренда:

| Токен | Hex | Назначение |
|-------|-----|------------|
| `like` / `--flora-like` | `#e8382c` | Лайки, ошибки |
| `gray` / `--flora-gray` | `#8f8f8f` | Приглушённый текст, иконки |

Производные (substrate, popover, bubble them, grid lines, dividers) — производные от брендовых и служебных; см. `:root` в `globals.css` и `FLORA_THEME_TOKENS`.

На web многие акценты дополнительно смягчаются через **`color-mix(in srgb, …)`** в CSS-модулях; на mobile эквivalent — предвычисленные токены или те же mix-формулы, не «чистый» `greenLight` там, где на web mix.