# Allowlisted packages (should skip registry)

Exact entry via install command; scoped `@flora/…` via lockfile
(install-token parser rejects `/` in tokens — scoped names come from lock).

```sh
npm install trusted-exact-pkg
npm install flora-helper-pkg
```
