//! Полигон качества FRC-A: детерминированный синтетический корпус,
//! объективные метрики, раннер по сетке битрейтов и арена сравнения
//! с внешними кодеками (Opus/AAC/MP3/Vorbis через ffmpeg).
//!
//! Назначение — инструмент разработки кодека (`Products/FRC`): регрессионные
//! quality-gates в CI (`tests/quality_gates.rs`), A/B-замеры изменений
//! энкодера/аллокации, снимки JSON для сравнения версий и трекинг позиции
//! против конкурентов (`run --vs opus,aac,mp3,vorbis`). ffmpeg — опциональная
//! зависимость машины разработчика: без него полигон работает в режиме
//! только-FRC-A. Никакой бизнес-логики Flora; зависимость только на
//! `frc-a-core` (направление зависимостей — полигон → кодек).

pub mod corpus;
pub mod external;
pub mod metrics;
pub mod report;
pub mod runner;
