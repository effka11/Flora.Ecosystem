# Flora.gRPC — protobuf contracts

C# gRPC host project removed (Phase 5). This tree keeps **`.proto` files only** —
shared wire contracts for Rust modules (e.g. `flora-verification` `build.rs`) and future microservices.

Do not add `.csproj` / generated C# stubs here.
