# PRODUCT_CLASS: functional
# FRC — Flora Relativistic Codec family (headless / embeddable)
#
# Spec: docs/codecs/
# Crates: frc-a-*, frc-i, frc-v*, flora-codec-tools — members of Backend/ workspace (not separate Codecs workspaces).
# Legacy path Codecs/ redirects here.
