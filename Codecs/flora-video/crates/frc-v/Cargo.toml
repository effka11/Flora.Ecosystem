[package]
name = "frc-v"
description = "FRC-V — Flora Relativistic Codec (video). Референсная реализация битстрима FRV1 (энкодер + декодер). Ядро без внешних зависимостей."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

# Ядро сознательно без зависимостей: аудируемость, тривиальная сборка в wasm32,
# нулевая supply-chain-поверхность. Не добавлять зависимости без веской причины.
[dependencies]

[lints]
workspace = true
