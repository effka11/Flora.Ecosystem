# FAC — Flora Audio Codec (битстрим FAC1)

**Status:** Draft  
**Version:** 0.3 (bitstream v0, не заморожен; 0.3 несовместим с 0.2 — энтропийный слой переведён с кодов Райса на бинарный range coder с адаптивными контекстами)  
**Date:** 2026-07-14  
**Depends on:** [`CODECS.md`](./CODECS.md), [`CODECS-AUDIO.md`](./CODECS-AUDIO.md)  
**Reference implementation:** `Codecs/audio/` (Rust, crates `fac-core`, `fac-cli`)

---

## Overview

FAC — собственный аудио-кодек экосистемы Flora. Цель программы — кодек, превосходящий по эффективности используемые сегодня AAC/Opus в целевых сценариях Flora: голосовые сообщения (E2E, клиентское кодирование) и музыка (серверный транскод).

Документ нормативен для битстрима v0: независимая реализация, следуя этому тексту, обязана декодировать поток эталонной реализации. До заморозки (v1) битстрим может меняться без обратной совместимости.

FAC **не изменяет** действующую политику `CODECS-AUDIO` v1 (AAC-контуры остаются в проде). Ввод FAC в контуры — отдельное решение после бенчмарков (см. Roadmap).

---

## Strategy (честная оценка)

| Этап | Что | Против кого | Ожидание |
| --- | --- | --- | --- |
| v0 (этот документ) | Классическое MDCT-ядро, рабочий сквозной кодек | — | Фундамент: формат, метрики, инфраструктура. Хуже Opus |
| v0.x | PVQ, психоакустика, транзиенты, range coder | AAC-LC | Сопоставимо на музыке средних/высоких битрейтов |
| v1 | Заморозка битстрима классического профиля | Opus/CELT | Паритет в узких сценариях |
| FAC-NC | Нейропрофиль: RVQ-кодовые книги + генеративный декодер | Opus, xHE-AAC, EVS | Превосходство на 3–24 kbps (голос, затем музыка) — именно здесь классика проигрывает нейрокодекам в разы |

Классическое ядро не выбрасывается после появления FAC-NC: оно остаётся (a) fallback-профилем для слабых устройств, (b) транспортом высоких битрейтов, (c) детерминированной базой для PLC.

---

## Architecture Position

```
Codecs/audio  (отдельный cargo-workspace, чистый Rust, no unsafe, no C deps)
  ├─ fac-core   — кодек: DSP + битстрим (готов к wasm32 / FFI / серверу)
  └─ fac-cli    — encode/decode/roundtrip/gen поверх WAV (инструмент разработки)

Потребители (будущая интеграция, направление зависимостей →):
  Apps/Web (wasm)        → fac-core   # голосовые E2E: сжатие ДО шифрования, как требует CODECS.md
  Apps/Mobile (FFI)      → fac-core
  Backend/flora-music    → fac-core   # серверный транскод музыки
```

Границы: `fac-core` не знает о модулях Flora, БД и HTTP; модули знают о нём только как о библиотеке. Для E2E-контура сервер продолжает видеть только шифроблоб — меняется лишь кодек внутри `voiceBlock` (будущий `contentType: audio/fac`).

---

## Codec overview (v0)

- Вход: PCM f32, 1–2 канала, 44.1/48 кГц (внутренняя сетка полос рассчитана на 48 кГц; на 44.1 края полос смещаются ~8% — допустимо для v0).
- Трансформа: MDCT, hop `N = 960` сэмплов (20 мс @ 48 кГц), низкоперекрывающееся окно (склейка `L = 120`, CELT-подход), overlap-add с шагом `N`, задержка кодек-цепочки `N`.
- **Транзиентный режим** (флаг в кадре): вместо одной длинной MDCT — 8 коротких MDCT `N/8 = 120` с интерливингом коэффициентов; устраняет pre-echo на атаках. Благодаря общей длине склейки `L` длинные и короткие кадры стыкуются без переходных окон и без лукахеда.
- Спектр делится на 21 полосу (шкала Барка), кодируются полосы до бина 800 (20 кГц @ 48 кГц); бины 800–959 не кодируются (нули).
- Каждая полоса — **gain-shape**: лог-энергия (gain) + нормированная форма (shape).
- Аллокация битов по полосам **не передаётся**: декодер выводит её из уже декодированных энергий тем же целочисленным алгоритмом, что и энкодер.
- Rate-контроль: глобальный индекс шага квантования `λ` (1 байт), подбирается энкодером под бюджет кадра.
- Стерео: фиксированное M/S.
- Полностью нулевая форма полосы → детерминированный noise-fill на декодере (кроме транзиентных кадров).
- PLC: повтор последнего спектра с затуханием в режиме последнего кадра.

### Нормативные формулы

Окно с носителем `n + l` по центру формальной длины `2n` (`l` — длина склейки, `s = (n − l)/2`, `rise(i) = sin((π/2)·sin²(π·(i+0.5)/(2l)))`):

```
w[m] = 0        для m ∈ [0, s) ∪ [2n−s, 2n)
w[s+i]      = rise(i),      i ∈ [0, l)
w[m] = 1        для m ∈ [s+l, 2n−s−l)
w[2n−s−l+i] = rise(l−1−i),  i ∈ [0, l)
```

Оно power-complementary (`w[m]² + w[m+n]² = 1`) и симметрично — условия Принсена–Брэдли выполняются. При `l = n` вырождается в классическое окно Vorbis.

- **Длинный кадр:** одна MDCT `n = N = 960`, `l = L = 120` над `[prev | cur]` (2N сэмплов).
- **Транзиентный кадр:** 8 коротких MDCT `n = N/8 = 120`, `l = 120` (полное перекрытие). Короткий блок `j ∈ [0, 8)` берёт сэмплы `[S₀ + 120·j, S₀ + 120·j + 240)` того же буфера `[prev | cur]`, где `S₀ = (N − L)/2 = 420`. Блоки точно тайлят носитель длинного окна; спад любого кадра встречает подъём той же формы `rise`, поэтому режимы свободно чередуются.

MDCT (вход `x[0..2n)` — windowed, выход `X[0..n)`) и iMDCT:

```
X[k] = Σ_{m=0}^{2n-1} w[m]·x[m]·cos( (π/n)·(m + 0.5 + n/2)·(k + 0.5) )
y[m] = (2/n)·w[m]·Σ_{k=0}^{n-1} X[k]·cos( (π/n)·(m + 0.5 + n/2)·(k + 0.5) )
```

Реконструкция — overlap-add кадров с шагом `N`; в транзиентном кадре 8 коротких синтезов суммируются в свои позиции формального 2N-окна, дальше тот же overlap-add.

**Интерливинг** (транзиентный кадр): коэффициент `f` короткого блока `j` хранится в спектре кадра по индексу `X[f·8 + j]`. Все края полос кратны 8, поэтому полоса `b` в интерливинг-домене покрывает те же индексы `[edges[b], edges[b+1])` — энергии, аллокация и форма не зависят от режима кадра.

Решение о транзиенте принимает энкодер; нормативен только флаг в битстриме. Эталонный детектор (не нормативен): скачок энергии первой разности сигнала в суб-блоке `N/8` более чем в 8 раз относительно максимума двух предыдущих суб-блоков (анализ текущего hop'а + хвоста предыдущего, все каналы). Эталонный энкодер не включает транзиентный режим при бюджете кадра < 512 бит: без noise-fill почти нулевая аллокация дала бы провалы звука.

M/S (обратимо, энергосохраняюще): `M = (L+R)/√2`, `S = (L−R)/√2`; обратно `L = (M+S)/√2`, `R = (M−S)/√2`. Плоскости кодирования: моно → `[X]`, стерео → `[M, S]`.

### Полосы (нормативная таблица, бины MDCT при N=960)

```
edges[22] = 0, 8, 16, 24, 32, 40, 48, 56, 64, 80, 96, 112, 128,
            160, 192, 224, 272, 320, 384, 480, 624, 800
```

Полоса `b` покрывает бины `[edges[b], edges[b+1])`, `b ∈ [0, 21)`.

### Энергии (gain)

Для плоскости `p`, полосы `b`: `E = Σ X[i]² + 1e-10`.

**Coarse** (управляет аллокацией): `q[p][b] = round(4·log2(E))` (шаг 0.25 log2 ≈ 0.75 дБ, i32). Кодирование: дельта к предыдущей полосе той же плоскости (первая полоса — дельта к 0), zigzag, бинаризация `k = 4` в контексте энергий (см. Энтропийное кодирование). Декодер ограничивает восстановленный `q` диапазоном `[−1024, 1024]` (защита от враждебного битстрима; честный энкодер этих значений не достигает). Coarse-gain: `ĝ = 2^(q/8)` (корень из энергии; используется для noise-fill полос).

**Fine** (только для кодируемых полос, β > 0): `u = clamp(⌊16·log2(E) − 4·q⌋ + 2, 0, 3)` — 2 бита, позиция внутри coarse-ячейки с точностью 1/16 log2 энергии. Fine-gain для масштабирования формы:

```
ĝ_fine = 2^( (8·q + 2·u − 3) / 64 )
```

Fine-биты пишутся в начале данных формы своей полосы (см. Кадр). Ошибка gain ≤ 1/64 log2 (≈ 0.05 дБ).

### Аллокация (общая целочисленная функция энкодера и декодера)

Reverse water-filling (равная дисторсия на коэффициент), целочисленно:

```
β_e8[p][b] = 0, если q[p][b] ≤ −132 (полоса на epsilon-полу — цифровая тишина)
иначе:
β_e8[p][b] = clamp( C + q[p][b] − LOG2W_X4[b], 0, 64 );  если β_e8 < 8 → β_e8 = 0

LOG2W_X4[b] = round(4·log2(width[b])) — нормативная таблица:
              12,12,12,12,12,12,12,12,16,16,16,16,20,20,20,22,22,24,26,29,30

C — наибольшее целое из [−3000, 3000], при котором
    Σ_{p,b} β_e8[p][b] · width[b]  ≤  S_e8       (бинарный поиск, монотонно)
```

Правило `β_e8 < 8 → 0` (не кодировать полосы дешевле 1 бит/коэфф) обязательно: стартовая цена нулевого коэффициента < 1 бита и при адаптации к нулям только падает, поэтому вместе с диапазоном `λ` оно гарантирует, что форма всегда помещается в бюджет. Превышение бюджета кадром возможно только когда `32 + 48 + биты энергий > budget` (ультранизкие битрейты — параметрический режим).

где `width[b] = edges[b+1] − edges[b]`, `S_e8 = 8 · shape_budget_bits` (бюджет формы кадра в 1/8 бита), `β_e8` — целевые биты на коэффициент в 1/8 бита (кап 8 бит/коэфф).

Бюджет кадра `budget = round(bitrate · N / sample_rate)` передаётся в заголовке кадра (u16le) — аллокация декодера обязана совпасть с энкодером бит-в-бит независимо от фактической длины пакета и транспорта. Учёт ведётся в 1/64 бита: бюджет формы (x64) = `64·budget − 64·(32 + 48) − стоимость coarse-энергий` (32 — заголовок, 48 — накладные range coder'а: ведущий байт + 5 байтов финализации; стоимость энергий — по нормативной функции стоимости битов, декодер накапливает её по фактически прочитанным символам). Бюджет аллокации `S = ⌊бюджет формы / 64⌋ − 2·planes·21` бит (резерв fine-битов). Всё с насыщением в 0.

**Правило пропуска:** полоса с `β_e8 == 0` не кодируется вовсе (0 бит) — декодер применяет к ней noise-fill. Это удерживает низкие битрейты: кадр не тратит ни бита на полосы вне бюджета.

### Форма (shape)

Нормировка: `s[i] = X[i]/g`, где `g = sqrt(E)` — истинный gain. Квантование равномерное: `y[i] = round(s[i]/step[b])`, кодирование zigzag + бинаризация с `k[b] = max(0, β_e8[p][b]/16 − 1)` *(целочисленно: `if β_e8 ≥ 16 { β_e8/8 − 1 } else { 0 }`)* в контексте формы `k[b]`.

Шаг — **точная** степень двойки с табличной мантиссой (без libm, бит-в-бит на всех платформах). Все степени вида `2^(m/64)` (целое `m`) вычисляются единой таблицей:

```
R = 2^(1/64) = 1.0108892860517005                        # нормативная f64-константа
MANT64[j] = (R^j как f64, последовательным умножением) → f32,  j ∈ [0, 64)
pow2_e64(m) = MANT64[m mod 64] · 2^(m div 64)            # div/mod — к −∞; порядок клампится в ±126
```

IEEE-умножение и приведение f64→f32 корректно округлены на любой платформе, поэтому таблица воспроизводима бит-в-бит. Через `pow2_e64` считаются coarse-gain (`m = 8q`), fine-gain (`m = 8q + 2u − 3`) и шаг формы:

```
step = pow2_e64( 8·(λ_idx − 32 − β_e8[p][b]) )
```

`λ_idx ∈ [0, 127]` (1 байт в кадре): энкодер выбирает минимальный `λ_idx` (самый тонкий шаг), при котором оценка стоимости формы (детерминированная симуляция контекстов, см. Энтропийное кодирование) ≤ бюджета формы. Стоимость нестрого убывает по `λ_idx` (шире шаг → меньше |y|), поэтому поиск — точная бисекция; при `λ_idx = 127` все `y = 0`, цена нуля < 1 бита ≤ `β_e8/8` на коэффициент — подходящий `λ_idx` существует всегда.

Декодер: `ŝ[i] = y[i]·step[b]`, `norm = sqrt(Σ ŝ²)`. Если `norm > 0`: `X̂[i] = ŝ[i]·(ĝ_fine/norm)` (энергия полосы восстанавливается точно в пределах fine-шага). Если вся полоса нулевая — noise-fill по coarse-gain `ĝ` **только в длинных кадрах**; в транзиентных кадрах полоса остаётся нулевой (шум без временнóй огибающей размазался бы на все 8 блоков и вернул pre-echo). Noise-fill:

```
seed = (frame_index·2654435761 ⊕ p·40503 ⊕ b·9973) | 1   # u32, wrapping
далее xorshift32 (x ^= x<<13; x ^= x>>17; x ^= x<<5) на каждый бин:
n[i] = (x / 2³¹) − 1.0  ∈ [−1, 1),  затем нормировка n к единичной норме и X̂ = n·ĝ
```

`frame_index` — порядковый номер кадра с начала потока (включая PLC-кадры), с нуля, u32 wrapping.

### Энтропийное кодирование: бинарный range coder

Всё после 4-байтового заголовка кадра кодируется бинарным range coder'ом LZMA-типа: 32-битный `range` (старт `2³²−1`), перенос через `low: u64`, байтовая ренормализация при `range < 2²⁴`, вероятности «бит = 0» с точностью 12 бит (масштаб 4096):

```
bound = (range >> 12)·prob0
бит 0: range = bound;    бит 1: low += bound; range −= bound
shift_low (при ренормализации): вытолкнуть старший байт low с распространением
переноса через кэш и цепочку 0xFF (классическая схема LZMA)
```

Поток RC: 1 ведущий байт (артефакт инициализации кэша), далее байты кода; финализация выталкивает 5 байтов. Декодер пропускает ведущий байт, читает 4 байта инициализации `code` и зеркалит арифметику; чтение за концом буфера — признак усечённого пакета (ошибка), недостающие байты читаются как 0.

**Адаптивные контексты.** Вероятность после каждого бита сдвигается к наблюдённому значению на 1/32 (`p0 += (4096−p0)>>5` при 0, `p0 −= p0>>5` при 1). Контексты живут в пределах кадра и сбрасываются на его границе (потеря пакета не рассинхронизирует стороны). Нормативные стартовые значения `prob0`: контекст энергий — 2867; контексты формы — по одному на каждое значение `k[b] ∈ [0, 7]`, все со стартом 2335. Сырые биты (fine, остатки бинаризации, эскейпы) кодируются с фиксированным `prob0 = 2048` (ровно 1 бит).

**Бинаризация значения** `u` (zigzag, `zigzag(v) = (v << 1) ^ (v >> 31)`): частное `qt = u >> k` — унарно в адаптивном контексте (`qt` битов «1», затем «0»), после чего `k` младших битов `u` сырыми битами MSB-first. Эскейп: после 24 битов «1» (без стоп-бита) `u` целиком пишется как 32 сырых бита.

**Нормативный учёт стоимости** (rate-контроль): стоимость бита с частотой `f` (та сторона `prob0`/`4096−prob0`, что наблюдена) равна `768 − log2_x64(f)` в 1/64 бита, где `log2_x64(f) = ⌊64·log2(f)⌋`, вычисленный целочисленным методом последовательных возведений в квадрат (усечение только занижает `log2`, поэтому оценка — верхняя граница; реализация — `qmath::log2_x64`). Энкодер и декодер обязаны эволюционировать контексты в учёте так же, как при записи/чтении.

### Кадр (packet)

```
byte 0:   flags — bit0 = transient, bit1..2 = 0 (резерв v0),
          bit3 = ms_stereo (1 для 2 каналов), bit4..7 = 0
byte 1:   λ_idx (0..127)
byte 2-3: budget u16le — бюджет кадра в битах (см. Аллокация)
далее — поток range coder'а:
  coarse-энергии всех плоскостей (плоскость за плоскостью, полосы по порядку),
  затем формы всех плоскостей (тот же порядок, полосы с β_e8 = 0 пропускаются):
    на кодируемую полосу — [fine u: 2 сырых бита][коэффициенты zigzag, бинаризация k[b]],
  финализация range coder'а (5 байтов)
```

Декодер обязан возвращать ошибку (не панику) на любой некорректный вход; усечение обнаруживается флагом переполнения RC до изменения состояния декодера.

### PLC (потеря пакета)

`decode_lost()`: последний декодированный спектр каждой плоскости умножается на 0.7 и синтезируется как обычный кадр **в режиме последнего кадра** (длинный/транзиентный). Состояние overlap-add продолжает жить — щелчков на границе нет.

---

## Контейнер FACS (файловый, для инструментов; транспорт Flora — свой)

```
магия "FACS" (4B) | version u8 = 1 | channels u8 | sample_rate u32le |
frame_n u16le = 960 | num_samples u64le (на канал, без задержки)
затем пакеты: len u16le | payload[len] ... до EOF
```

Декодер файла отбрасывает первые `N` сэмплов (задержка) и обрезает хвост до `num_samples`. В FSCP/HTTP пакеты передаются собственным транспортом — контейнер не обязателен.

---

## Метрики и бенчмарки

- v0: SNR (время) и фактический kbps — `fac roundtrip`; band-LSD и pre-echo (RMS ошибки в окне 13 мс перед атакой, A/B транзиентного режима) — интеграционные тесты `fac-core`.
- v0.x: ViSQOL (объективное перцептивное качество) + слепые ABX; сравнение с Opus/AAC на корпусе (речь: чистая/шумная; музыка: разные жанры; edge: аплодисменты, клавесин, кастаньеты).
- Тест-векторы FAC добавляются в `docs/test-vectors/` **только генерацией** из эталонной реализации (правило репозитория).

## Module boundaries

- Кодек — только `Codecs/audio` (`fac-core`). Модули/приложения используют его как библиотеку; обратных зависимостей нет.
- Транскод-пайплайны остаются в своих владельцах (`Flora.Music`, `Apps/Web`) — FAC не двигает границы `CODECS.md`.
- E2E-инвариант сохраняется: голосовые кодируются на клиенте до шифрования.

## Roadmap

1. ~~**v0.1** — транзиент-детектор + короткие окна (8×120)~~ — сделано (v0.2 спецификации).
2. ~~**v0.2** — range coder вместо Rice, адаптивные контексты~~ — сделано (v0.3 спецификации).
3. **v0.4** — PVQ вместо скалярного квантования (поверх RC: равномерные символы + theta-split), психоакустическая модель маскирования, per-band M/S, anti-collapse для транзиентных кадров, быстрая MDCT (FFT-путь для DCT-IV), ресемплер, VBR; компактная финализация RC (CELT-стиль) для ультранизких битрейтов.
4. **v1** — заморозка битстрима, фиксированная точность декодера (бит-в-бит кросс-платформенно), fuzzing-кампания, тест-векторы, WASM/FFI-обвязка.
5. **FAC-NC** — нейропрофиль: RVQ-энкодер/декодер (обучение — PyTorch, вне этого workspace; инференс — Rust `candle`/`tract`, int8), голос 3–8 kbps, музыка 12–24 kbps. Гейт на ввод в прод: слепые тесты против Opus на целевых битрейтах.
6. **Интеграция** — `voiceBlock.contentType = audio/fac` (FSCP, клиентское кодирование), ladder Flora.Music; обновление `CODECS-AUDIO.md` отдельным PR.

## Open Questions

- Детерминизм декодера FAC-NC (float-инференс) между платформами — исследование в фазе NC (кандидат: полностью int8-путь).
- Единый мультимедиа-контейнер с видео/фото-кодеками (агенты FVC/FIC) — после стабилизации всех трёх битстримов.
- Низколатентный режим (5/10 мс) для звонков — после v0.2.
