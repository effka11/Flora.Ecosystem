# FPP-SIGNALS — метрики натуральности аккаунтов и устройств

**Status:** Draft (RFC)
**Version:** 0.3
**Date:** 2026-08-02

Компаньон [`FPP.md`](./FPP.md) (v0.3; протокольная привязка слоя — там, §8.3). Потребители: программа V-01 ([`FGP-THREATS.md`](../fgp/FGP-THREATS.md) — детекция сибил-ферм и аренды) и замер `C_identity` (FPP §7). Криптографические деривации — [`FGP-CRYPTO.md`](../fgp/FGP-CRYPTO.md) (реестр доменных меток §1.1). Реализация — детерминированное ядро `fpp-core` + `fpp-crypto` (`Products/FPP`); данные владеет модуль Verification (FPP §10.2). История адверсариальных ревью — Приложение B.

---

## Overview

FPP делает подделку личности **дорогой**; NS-слой (naturalness signals) делает её **заметной раньше, чем она окупится**. Вопрос слоя: «похоже ли поведение аккаунта и его устройств на поведение одного живого человека — и сколько стоит имитировать это на масштабе `k` личностей?»

Три обязательства, отличающие NS от коммерческого антифрода:

1. **Advisory-only.** Ни одна метрика не деградирует уровень, не блокирует права и не выносит вердикт. Выходы NS — приоритет следственной очереди V-01, вероятность назначения канареечной пары (FPP §3.1) и отдельная строка замера `C_hat` (FPP §7.2). Санкция всегда требует человеческого вердикта по FGP §4.1.4 — принцип FPP §8.2 «сигнал к следствию, не автоматический приговор» нормативен для всего слоя.
2. **Приватность по построению.** Сырые поведенческие события не покидают устройство; сервер видит только грубые квантованные bucket'ы, всегда-агрегатные потоки и то, что наблюдает сам (§3). Хранимое намеренно бедно: нечего изъять, слить или продать (THREATS V-08).
3. **Детерминизм.** Все метрики целочисленные (промилле), воспроизводимы бит-в-бит на native и wasm32; спецификация арифметики — golden-вектор `personhood-naturalness-v1.json`, не формулы на вещественных числах (правило FGP-CRYPTO §10).

## Goals & Non-Goals

**Goals:** реестр метрик NS-* с классами доказательности; модель данных и приватности (что хранится, что не существует, TTL, доступ); детерминированный скоринг с гистерезисом; анти-дискриминационные инварианты; экономика обхода и её замер.

**Non-Goals:**

- **Не** контент-модерация и не anti-spam ленты: качество и спамность контента — домен Content/FIRA, NS туда не экспортируется.
- **Не** биометрия и не поведенческая биометрия высокого разрешения: клавиатурный почерк как средство *идентификации* запрещён Non-Goals FPP; NS-C1 использует только грубые классы аномальности в момент церемонии.
- **Не** замена церемоний и поручительств: NS — телеметрия вокруг лестницы V0–V3, а не ещё одна ступень.
- **Не** автоматическая санкция (см. обязательство 1).

---

## 1. Классы доказательности

Вес сигнала определяется тем, **кто его наблюдал**, а не тем, что он утверждает. Клиент подконтролен атакующему — самоотчёты не могут перевешивать серверные наблюдения ни при какой калибровке (инвариант весов, Приложение A).

| Класс | Источник | Примеры | Доверие |
| --- | --- | --- | --- |
| **A — server-observed** | наблюдения самого Verification | конкурентность девайс-тегов, потоки регистраций/поручительств, исходы и графы церемоний, частота восстановлений (FPP §6) | высшее: подделка требует изменить реальное поведение, а не отчёт о нём |
| **B — device-attested** | вычислено на устройстве с hardware-backed ключом / платформенной аттестацией (NS-D1) | классы аномальности ввода в церемонии (NS-C1) | среднее: аттестация повышает цену эмуляции, но не исключает её |
| **C — self-reported** | bucket-профили, посчитанные клиентом без аттестации | темпоральные bucket'ы NS-T1/NS-T2 с web-клиента | advisory; **расхождение C с A — самостоятельный сигнал** (ферма, рапортующая «идеальную естественность» при машинных серверных наблюдениях, выдаёт себя самим отчётом) |

---

## 2. Реестр метрик

Нормативные формулы — реализация `fpp-core`, зафиксированная вектором (§7); ниже — семантика и экономика каждой метрики. Все пороги, кривые и веса — R2-параметры (Приложение A).

### NS-T1 — burstiness межсобытийных интервалов (класс C, на устройстве; класс A для серверных событий)

`B = (σ − μ)/(σ + μ)` по Гоху–Барабаши, в промилле `[−1000, 1000]`. Человеческая активность bursty (`B > 0`): пачки действий, длинные паузы. Расписания и cron-автоматика регулярны (`B → −1000`); пуассоновский случайный джиттер ≈ 0 — «рандомизация» скриптом сдвигает метрику к нулю, но не к человеческому положительному хвосту. Вырожденный случай `σ + μ = 0` определён как `−1000`. Минимальная выборка — 8 интервалов, иначе сигнала нет (§6); интервал зажимается 10 годами (детерминизм разрядности).

### NS-T2 — суточный профиль (класс C; класс A для событий, видимых серверу)

По 24-часовой гистограмме локального времени (сама гистограмма не покидает устройство, §3):

- **NS-T2a rest-share:** доля активности в самом тихом циклическом 6-часовом окне, ‰. У человека есть окно сна: значение мало; круглосуточная равномерность даёт ≈ 250‰ (по принципу Дирихле больше 250‰ не бывает). Окно циклическое — «сон через полночь» и любой сменный график находятся одинаково.
- **NS-T2b peak-share:** доля пикового часа, ‰. Неестественны **оба** хвоста: ≈ 42‰ — идеальная равномерность (машина), высокие значения — концентрация в один час (cron-пакет). Человеческий пик обычно 80–250‰.
- **NS-T2c self-similarity:** пересечение гистограмм двух эпох одной личности (`Σ min(aᵢ, bᵢ)·1000 / max(Σa, Σb)`, ‰). Почти ноль — маркер передачи аккаунта между операторами (аренда, FPP §8.2); **слишком высокое — тоже аномалия** (replay одного шаблона): калибровочная кривая немонотонна.

### NS-T3 — CUSUM агрегатных потоков (класс A, сервер)

Односторонний CUSUM `s ← max(0, s + x − k)`, тревога при `s ≥ h`, по потокам счётчиков: регистрации, выдача поручительств, декларации слотов церемоний. Реализует «темпоральную кластеризацию (CUSUM по потоку)» из THREATS V-01 как нормативный алгоритм. Входы — только счётчики за такт, персональных данных в метрике нет.

### NS-D1 — класс аттестации устройства (класс B)

`Unattested | SoftwareKey | HardwareBacked` — доступность hardware-backed ключа/платформенной аттестации. **Отсутствие аттестации нейтрально** (§6): web-клиент не штрафуется; класс лишь определяет вес девайс-сигналов этой платформы. Нормативные правила весов (`fpp-core::profile`):

- **самоотчёты** (темпоральные bucket'ы с устройства): `HardwareBacked` → класс B, иначе — класс C; самоотчёт никогда не достигает класса A;
- **девайс-наблюдения NS-D2/NS-D3 — асимметрично**: инкриминирующий исход (коллизия тегов `Shared`/`FarmSuspect`, текучка `Mobile`/`Churning`) — всегда класс A: сервер видел факт, аттестация не требуется; экскульпирующий исход (`Exclusive`/`Stable`) стоит ровно столько, сколько стоит чеканка девайс-идентичности: `HardwareBacked` → A, `SoftwareKey` → B, `Unattested` → C. Без асимметрии ферма без аттестации «самозаверяла» бы невиновность свежим software-ключом на каждый аккаунт (Приложение B.2).

### NS-D2 — конкурентность устройства (класс A)

Эпохальный тег устройства: `device_tag = BLAKE3-derive("flora/device/v1/tag", pk_device ‖ epoch_id)` (`fpp-crypto::device_tag_epoch`; метка — в реестре FGP-CRYPTO §1.1). Вычисляется на устройстве; сервер хранит **только тег**. Метрика — число активных civic-личностей, предъявивших один тег в эпохе: `Exclusive (1) | Shared (2–3, легитимно: домохозяйство) | FarmSuspect (≥ 4)`.

- **Каноническая эпоха** (`fpp-core::epoch`, нормативно): `epoch_index = ⌊(unix − genesis)/len⌋` (до genesis и при `len = 0` эпохи нет — конфиг отклоняется), `epoch_id = LE64(genesis_unix_s) ‖ LE64(epoch_index)` — 16 байт. Деривация обязана совпадать байт-в-байт у клиента и сервера, иначе теги не сойдутся; включение genesis (публичный R2-параметр инсталляции) разводит теги разных инсталляций по построению.
- **Ротация с эпохой:** тег меняется каждую эпоху (90 дней) — конкурентность внутри эпохи наблюдаема, долговременный трекинг устройства невозможен без `pk_device`, который серверу не предъявляется.
- **Экономика:** это самый дорогой для фермы сигнал класса A на аттестованных платформах — эмулировать много «независимых» hardware-backed устройств на одном железе дорого, а покупать физические девайсы — линейная статья в `C_identity` (§5).

### NS-D3 — текучка устройств личности (класс A)

Число разных девайс-тегов одной личности за эпоху: `Stable (≤ 2) | Mobile (3–4) | Churning (≥ 5)`. Высокая ротация в сочетании с частыми восстановлениями (приватный флаг FPP §6) — маркер аренды/кражи личности.

### NS-B1 — межаккаунтная монотонность (класс A, только следственный контур)

Сходство суточных профилей и корреляция активности **между** аккаунтами (та же функция пересечения, что NS-T2c). Массовая аренда/ферма даёт поведенческую монотонность — один оператор оставляет один ритм на всех подконтрольных аккаунтах (FPP §8.2). Попарные сравнения по всей популяции запрещены (§3): метрика вычисляется только внутри следственного скоупа, открытого вердиктом (кандидатный кластер из NS-T3/NS-D2/графовых аномалий), с журналированием доступа — как доступ к рёбрам поручительств (FPP §4.3).

### NS-C1 — enum-флаги аномалий церемоний (класс B)

Нормативный реестр кодов к FPP §3.1 («на сервер уходят только подписанные вердикты и enum-флаги аномалий»; свободный текст запрещён — канал утечки PII, FPP §9.1). Коды wire-stable (`fpp-contracts::CeremonyAnomalyFlag`, вектор §7): `latency_suspect`, `synthetic_media_suspect`, `audio_video_desync`, `challenge_mismatch`, `input_cadence_anomaly`, `participant_aborted`, `abuse_reported`, `partner_no_show`. Код 0 зарезервирован («нет аномалии») и не сериализуется. Паттерн флагов по когортам — вход детекции V-01 (deepfake-конвейер даёт систематическую латентность и десинхрон).

### NS-G — графовые аномалии

Аналитика графов поручительств и пар церемоний нормирована в FPP §4.3 и §3.1 — NS их **не дублирует**; сюда входит только темпоральный слой (NS-T3) над теми же потоками.

---

## 3. Модель данных и приватность

Расширение таблицы FPP §9.1 (исчерпывающе для NS):

| Данные | Где | TTL | Не существует нигде |
| --- | --- | --- | --- |
| Bucket-профиль натуральности личности: квантованные классы NS-T1/T2 (5 уровней на метрику), классы NS-D1/D3, скор и класс §4 | Verification, `personhood_naturalness` | 12 мес | сырые события, точные ‰-значения с устройства, суточные гистограммы |
| Эпохальные девайс-теги + счётчик конкурентности | Verification, `personhood_naturalness` | эпоха + 6 мес | `pk_device`, серийники/фингерпринты устройств, кросс-эпохная связка тегов |
| Флаги NS-C1 при вердиктах церемоний | уже нормировано FPP §9.1 (вердикты пар) | 24 мес | медиа, свободный текст |
| Агрегаты когорт (распределения классов, доля тревог NS-T3, FPR-аудит §6) | публикуются | — | агрегат когорты < `k_min` (правило слияния страт FGP-CRYPTO §5) |

- **Квантование как защита:** на сервер уходят bucket'ы из 5 уровней, а не точные значения — хранимый профиль слишком груб для поведенческого фингерпринтинга и деанонимизации, но достаточен для приоритизации следствия. Нормативные границы bucket'ов и репрезентативные точки — Приложение A (`fpp-core::profile`, R2); натуральность bucket'а вычисляется **сервером** как калибровочная кривая в репрезентативной точке — пере-калибровка кривых (R2) применима к уже хранимым bucket'ам без пересбора данных с устройств.
- **Доступ:** скор и класс — модулю Verification; попарные метрики NS-B1 и сырые bucket-профили — только следственной панели по вердикту, с журналированием (симметрично рёбрам поручительств FPP §4.3). Governance видит только k-анонимные агрегаты когорт.
- **Право на удаление:** выход (FPP §6, §9.2) удаляет NS-данные вместе с остальным содержимым модуля.
- **Границы модулей:** NS не читает чужих таблиц. Паттерны расходования кредитов (V-03) остаются в Governance; корреляционный стык — только на уровне следственной комиссии, не на уровне данных.

### 3.1. Отчёт эпохи устройства (нормативный wire-контракт)

Единственное, что клиент отправляет серверу по NS-слою, раз в эпоху, — `fpp-contracts::NaturalnessEpochReport`:

| Поле | Содержимое |
| --- | --- |
| `epoch_index` | индекс отчётной эпохи по канонической деривации (§2 NS-D2); актуальность (не прошлая/не будущая) проверяет Verification по своим часам |
| `device_tag` | 32-байтовый эпохальный девайс-тег (§2 NS-D2) |
| `temporal_buckets` | самоотчётные темпоральные bucket'ы NS-T1/NS-T2 — пары «метрика, bucket» |

Правила формы (валидация на границе приёма, `fpp-core::profile::temporal_from_report`; нарушение → отчёт отклоняется целиком, негативный вектор §7):

- только темпоральные метрики: девайс-наблюдения NS-D2/NS-D3 сервер делает сам — самоотчёт по ним не принимается ни с каким весом (`non_temporal_metric`);
- канонический порядок: коды метрик строго возрастают, дубли запрещены (`duplicate_metric`, `out_of_order`) — перестановки строк не образуют скрытый канал;
- отсутствующая метрика просто не включается — пустой список валиден (нейтральное отсутствие, §6);
- **класса аттестации NS-D1 в отчёте нет намеренно**: самодекларация «hardware_backed» ничего не стоила бы (клиент подконтролен атакующему, §1) — класс присваивает Verification по верифицированной регистрации девайс-ключа и хранит рядом с отчётом.

---

## 4. Скоринг и классы

1. Сырая метрика → **калибровочная кривая** (кусочно-линейная, целочисленная, немонотонность разрешена) → натуральность сигнала в ‰ (1000 = максимально естественно). Стартовые кривые — Приложение A; изменение — R2.
2. Сигналы сводятся **взвешенным средним** с весами по классу доказательности (A=3, B=2, C=1 на сигнал; R2). Отсутствующий сигнал не участвует — вес перенормируется по доступным (правило нейтрального отсутствия §6). Пустой набор → скора нет.
3. Классификация с гистерезисом: `Natural (≥ 600) | Watch (≥ 350) | Investigate (< 350)`, полоса гистерезиса 50‰ — смена класса требует пробить порог на 50‰ в сторону смены; дрожание скора вокруг порога не «мигает» классом.

Роль классов (исчерпывающе):

- `Watch` — повышенная вероятность назначения канареечного участника в церемонию (FPP §3.1) и приоритет в выборке ретестов;
- `Investigate` — верх следственной очереди V-01; открытие следственного скоупа NS-B1 — **только** вердиктом панели, не классом;
- страта для замера `C_hat`: red team обязана мерить цену производства личности, проходящей NS в `Natural` (§5).

Чем классы **не являются**: основанием деградации уровня, блокировки прав, отказа в церемонии, публичной меткой аккаунта.

### 4.1. Отчёт панели: нормативный реестр переменных

Исчерпывающий набор переменных, который следственная панель V-01 / панель модерации видит по одной civic-личности. Wire-shape — `fpp-contracts::NaturalnessPanelReport`; детерминированная сборка — `fpp-core::profile::assemble` (bucket-профиль + девайс-счётчики + серверные счётчики; сырые события для сборки не нужны и не существуют, §3). Personhood-уровень V0–V3 и состояние аттестации (FPP §2) в отчёт **не входят** — это домен Verification, панель показывает их рядом с отчётом.

| Переменная | Значения | Класс | Что говорит панели |
| --- | --- | --- | --- |
| `scorePermille` | 0..1000 ‰; `None` — сигналов нет | свод | сводная натуральность; отсутствие — не штраф (§6) |
| `class` | `natural` / `watch` / `investigate` | свод | приоритет следствия и ретестов (§4); advisory-only |
| `evidenceShares` | доли веса A/B/C в скоре, ‰ | мета | «на чём стоит скор»: 900‰ из самоотчётов ≠ 900‰ из серверных наблюдений |
| `signals[]` | метрика + класс доказательности + bucket + натуральность ‰ | A/B/C | все строки, вошедшие в свод, — скор исчерпывающе объясним |
| `reportConsistency` | `consistent` / `drifting` / `contradictory` | мета | расхождение самоотчёта с серверными наблюдениями — самостоятельный сигнал (§1): ферма, рапортующая «идеал», выдаёт себя |
| `deviceAttestation` (NS-D1) | `unattested` / `software_key` / `hardware_backed` | B | вес девайс-сигналов; сам по себе нейтрален (§6) |
| `deviceLink` + `concurrentCivicOnDevice` (NS-D2) | `exclusive` / `shared` / `farm_suspect` + счётчик | A (экскульпирующее — по NS-D1) | личностей на девайсе; `shared` легитимен (домохозяйство) |
| `deviceChurn` + `distinctDevicesEpoch` (NS-D3) | `stable` / `mobile` / `churning` + счётчик | A (экскульпирующее — по NS-D1) | текучка девайсов — маркер аренды/кражи (FPP §8.2) |
| `ceremoniesCompleted24m` / `ownFails24m` / `ownNoShows24m` | счётчики за TTL 24 мес | A | паттерн собственных фейлов (FPP §2, §8.1); чужие no-show не засчитываются (FPP §3.1) |
| `verdictReliabilityPermille` | 0..1000 ‰; `None` — мало истории | A | надёжность выдающего вердикты (FPP §3.1): гриферство обесценивает вердикты грифера |
| `recoveries12m` | счётчик | A | частые восстановления — маркер аренды/кражи личности (FPP §6) |
| `anomalyFlags24m` | счётчики по реестру NS-C1 | B | систематическая латентность/десинхрон — паттерн deepfake-конвейера (FPP §3.2) |

Правила чтения (нормативно):

- Отчёт **advisory-only** (FPP §8.3): он приоритизирует работу панели — санкция всегда требует человеческого вердикта по FGP §4.1.4.
- Идентификатора субъекта в отчёте нет намеренно; подшивку «отчёт ↔ civic_id» держит Verification, доступ — следственный, с журналированием (§3).
- Согласованность считается по метрикам, наблюдаемым **обеими** сторонами: максимальная дистанция bucket'ов; пороги — Приложение A.
- Отсутствие любой переменной (`None`) нейтрально (§6) — интерпретация «нет данных = подозрительно» запрещена.

---

## 5. Экономика обхода и Goodhart (честно)

- **Что дорожает.** Дешёвая ферма (один оператор, скрипты, пачка аккаунтов на устройство) ловится классом A: конкурентность девайс-тегов (NS-D2), CUSUM всплесков (NS-T3), монотонность (NS-B1). Чтобы пройти в `Natural`, ферме нужно: физическое устройство на каждые ≤ 3 личности, независимые «живые» ритмы с правдоподобной эволюцией эпоха-к-эпохе, разнесённые во времени регистрации и поручительства. Это линейные статьи в `C_identity(L)` сверх церемоний.
- **Что остаётся дёшево.** Класс C симулируется бесплатно — поэтому он advisory и никогда не перевешивает A (§1). Идеальная симуляция всех классов возможна: NS — **пол, поднимающий цену дешёвого хвоста, а не потолок**. Потолок по-прежнему задают синхронные церемонии (FPP §3) и зарплата соучастника при аренде (FPP §8.2).
- **Замер вместо веры.** `C_hat` получает отдельную строку: «стоимость natural-looking фермы» — производственная оценка red team сибил-кольца, проходящего NS в `Natural` (методика FPP §7.2). Если строка сходится к обычной себестоимости — NS не работает, и это будет видно в квартальном отчёте.
- **Goodhart на метриках.** Параметры публичны (прозрачность R2) — атакующий может оптимизироваться под них. Ответ: (a) метрики измеряют дорогие в имитации инварианты (одновременность устройств, годовую эволюцию ритма), а не декоративные признаки; (b) канареечные фермы red team калибруют evasion-стоимость дважды в год (каденция сибил-учений V-01); (c) советующая, а не карающая роль убирает мотив «оптимизироваться до миллиметра» — пройти NS недостаточно, церемонии никуда не деваются.

---

## 6. Анти-дискриминационные инварианты (нормативно)

Скошенная метрика = скошенный электорат — это integrity-требование, не только этика (тот же принцип, что FPP §3.1.1):

1. **Форма, не объём.** Все метрики нормированы долями (‰) — мало пользоваться сервисом не значит быть ботом. Абсолютный объём активности в скор не входит.
2. **Нейтральное отсутствие.** Ниже минимальной выборки сигнал = `None`, вес перенормируется; отсутствие аттестации устройства (web) — нейтрально. Отсутствие данных никогда не понижает скор.
3. **Сменные графики и часовые пояса.** Окно отдыха циклическое (любые 6 часов подряд); ротация смен снижает self-similarity умеренно — полоса `Natural` кривой NS-T2c широка, флагуется только вырожденное несходство (передача аккаунта), не «нестабильная жизнь».
4. **Доступность.** Пользователи асинхронного пути (FPP §3.3) проходят NS на общих основаниях — их челленджи и так внутри декларированных окон бодрствования; никаких отдельных порогов для людей с инвалидностью.
5. **FPR-аудит.** Ежегодно (вместе с аудитом доступности V-01) публикуются k-анонимные доли `Watch`/`Investigate` по когортам (стаж, платформа, асинхронный путь, регион-самодекларация FPP §4.1). Статзначимый перекос против когорты → пере-калибровка кривых (R2) с постмортемом.

---

## 7. Реализация

- **Ядро:** `Products/FPP/crates/fpp-core` — модули `temporal` (NS-T1/T2), `streams` (NS-T3), `device` (NS-D2/D3), `epoch` (каноническая эпоха и `epoch_id`, §2 NS-D2), `piecewise` (калибровочные кривые + `validate`: пустая кривая, `y` вне ‰, неубывающий `x` — отклоняются), `score` (свод, классы, гистерезис), `profile` (5-уровневый bucket-профиль: квантование `TemporalBuckets::quantize_raw`, строки отчёта `to_report_buckets` / валидация `temporal_from_report` (§3.1), правила весов NS-D1, согласованность A↔C, сборка отчёта панели `assemble`). Целочисленная арифметика: промилле, деление — усечение к нулю, корень — целочисленный floor; без float (инвариант FGP §8.1); wasm32-переносимо, зависимостей на модули workspace нет.
- **Крипто:** `fpp-crypto::device_tag_epoch` поверх реестра доменных меток `flora-governance-crypto` (`flora/device/v1/tag`, FGP-CRYPTO §1.1).
- **Контракты:** `fpp-contracts` — wire-stable коды всех реестров: `PersonhoodLevel`, `NaturalnessClass`, `SignalEvidenceClass`, `DeviceAttestationClass`, `DeviceLinkClass`, `DeviceChurnClass`, `CeremonyAnomalyFlag`, `SignalMetric`, `SignalBucket`, `ReportConsistencyClass`. Правило кодов: в реестрах **классов** код `0` зарезервирован («не задано») и не сериализуется; порядковые шкалы (`PersonhoodLevel` — номер уровня, `SignalBucket` — величина 0..=4) сохраняют смысловые коды, включая 0; неизвестный код — отказ (`from_code → None`), не «ближайшее» значение. DTO: отчёт панели `NaturalnessPanelReport` / `PanelSignal` / `PanelCounters` / `EvidenceShares` (§4.1) + отчёт эпохи `NaturalnessEpochReport` / `ReportedBucket` (§3.1).
- **Клиент (TS):** `@flora/client-core/personhood` — зеркало клиентского пути: реестры, метрики NS-T1/NS-T2, квантование, каноническая эпоха, девайс-тег (BLAKE3 derive_key через `@noble/hashes`), сборка отчёта эпохи `buildEpochReport` и валидация `temporalFromReport`. Серверные секции (CUSUM, кривые, свод, панель) клиент не зеркалит.
- **Данные:** таблица `personhood_naturalness` — схема `flora_core`, владелец Verification, миграции через `flora-migrate` (FPP §10.2). Появляется вместе с интеграцией в модуль (P1).
- **Test vectors:** `Documents/test-vectors/personhood/personhood-naturalness-v1.json` — wire-реестры (секция `registries`), все метрики, кривые, свод/классификация, bucket-профиль и отчёт панели (секция `panel`), каноническая эпоха и эпохальный девайс-тег (секция `epoch`), клиентский путь отчёта эпохи (секция `epochReport`); негативный вектор `personhood-naturalness-negative-v1.json` — обязательные отказы (форма отчёта эпохи, неизвестные wire-коды, невалидные кривые). Регенерация `cargo run -p fpp-core --example gen_personhood_vectors`; consumer-тесты: Rust `fpp-core/tests/naturalness_vectors.rs`, TS `Packages/flora-client-core/src/personhood/personhoodVectors.test.ts`.
- **Порядок внедрения (P1, FPP §11):** ядро и вектора (готово) → квантование bucket'ов и отчёт панели в ядре (готово, §4.1) → расчёт bucket'ов в клиентах + отчёт эпохи (готово: контракт §3.1, TS-поверхность `@flora/client-core/personhood`) → таблица и приём отчётов в Verification → канареечная калибровка кривых в песочнице → включение в квартальный отчёт `C_hat`.

---

## Приложение A. Параметры (стартовые значения; класс изменения R2)

| Параметр | Значение | Раздел |
| --- | --- | --- |
| Шкала | промилле (‰), целые; деление — усечение к нулю; isqrt — floor | §7 |
| NS-T1: минимум интервалов / зажим интервала | 8 / 10 лет | §2 |
| NS-T2: минимум событий профиля / окно отдыха | 24 / 6 ч (циклическое) | §2 |
| NS-T3: reference `k`, порог `h` | на поток, калибруются в v0 (стартовый вектор: k=5, h=20 на такт) | §2 |
| NS-D2: пороги Shared / FarmSuspect | 2 / 4 личности на тег-эпоху | §2 |
| NS-D3: пороги Mobile / Churning | 3 / 5 девайсов за эпоху | §2 |
| Эпоха девайс-тега | 90 дней (= эпоха поручительств FPP §4.1) | §2 |
| Каноническая эпоха: genesis инсталляции | публичный R2-параметр; `epoch_id = LE64(genesis) ‖ LE64(index)` | §2 |
| Форма отчёта эпохи | только темпоральные метрики, коды строго возрастают, пусто — валидно | §3.1 |
| Веса классов доказательности A/B/C | 3 / 2 / 1 | §4 |
| Пороги классов: Natural / Watch / Investigate | ≥ 600 / ≥ 350 / < 350 ‰; гистерезис 50 ‰ | §4 |
| Калибровочные кривые NS-T1/T2a/T2b/T2c | точки — `fpp-core::score::{BURSTINESS,REST_SHARE,PEAK_SHARE,SELF_SIMILARITY}_CURVE`, зафиксированы вектором | §4 |
| Квантование bucket-профиля | 5 уровней на метрику | §3 |
| Границы bucket'ов NS-T1 / репрезентативные точки | −600/−200/200/600 ‰ / −800, −400, 0, 400, 800 | §3 |
| Границы bucket'ов NS-T2a / репрезентативные точки | 25/75/150/220 ‰ / 10, 50, 110, 185, 240 | §3 |
| Границы bucket'ов NS-T2b / репрезентативные точки | 60/90/260/550 ‰ / 40, 75, 170, 400, 750 | §3 |
| Границы bucket'ов NS-T2c / репрезентативные точки | 200/400/850/950 ‰ / 100, 300, 600, 900, 980 | §3 |
| Натуральность девайс-классов | NS-D2: 1000/800/100 ‰; NS-D3: 1000/650/150 ‰ | §2, §4.1 |
| Вес самоотчёта по NS-D1 | `hardware_backed` → B; иначе C | §2 |
| Вес девайс-наблюдений NS-D2/D3 | инкриминирующее → A; экскульпирующее: hw → A, software → B, unattested → C | §2 |
| Согласованность A↔C | max дистанция bucket'ов: ≥ 2 → `drifting`, ≥ 3 → `contradictory` | §4.1 |
| TTL: bucket-профиль / девайс-теги | 12 мес / эпоха + 6 мес | §3 |
| k-анонимность агрегатов | `k_min` = 20 (совместно с FGP-CRYPTO Приложение A) | §3 |
| FPR-аудит когорт | ежегодно, с аудитом доступности V-01 | §6 |
| Канареечная калибровка evasion-стоимости | 2 раза/год (каденция сибил-учений V-01) | §5 |

## Приложение B. История адверсариальных ревью

### B.1. Разбор черновика 0.1 (до фиксации; закрыто в самом 0.1)

| # | С. | Дыра в черновике | Закрытие |
| --- | --- | --- | --- |
| S1 | В | Телеметрия с подконтрольного клиента доказывает ноль: ферма рапортует идеальные bucket'ы | §1 классы доказательности A/B/C: самоотчёты advisory и не перевешивают серверные наблюдения ни при какой калибровке; расхождение C↔A — самостоятельный сигнал |
| S2 | В | Поведенческий профиль на сервере = новый канал деанонимизации и цель изъятия (противоречие V-08 «выдавать нечего») | §3: сырые события и гистограммы не покидают устройство; хранение — 5-уровневые bucket'ы; TTL 12 мес; попарные метрики — только следственный скоуп по вердикту с журналированием |
| S3 | В | Стабильный тег устройства = долговременный трекинг девайсов | §2 NS-D2: эпохальная ротация тега (PRF с `epoch_id`); кросс-эпохная связка без `pk_device` невозможна; `pk_device` серверу не предъявляется |
| S4 | В | Дискриминация нетипичных ритмов: сменная работа, инвалидность, низкая активность, часовые пояса | §6 инварианты: нормировка формой, а не объёмом; `None` при малой выборке нейтрален; циклическое окно отдыха; широкие полосы кривых; ежегодный FPR-аудит по когортам с пере-калибровкой |
| S5 | С | Goodhart: публичные параметры позволяют отрепетировать «естественность» | §5: честный статус пола (не потолка); измеряются дорогие инварианты (устройства, годовая эволюция ритма); канареечные фермы замеряют evasion-стоимость; отдельная строка `C_hat` делает деградацию слоя публично видимой |
| S6 | С | Web-клиенты без аттестации получали систематический минус — скос электората по платформе | §6 правило нейтрального отсутствия: отсутствие сигнала/аттестации не понижает скор, вес перенормируется по доступным сигналам |
| S7 | С | Идеально стабильный профиль эпоха-к-эпохе выглядел максимально «естественно» — replay одного шаблона проходил лучше живого человека | §2 NS-T2c и §4: калибровочные кривые немонотонны; сходство ≈ 1000 — аномалия наравне с несходством ≈ 0 |
| S8 | С | Классификация без гистерезиса «мигала» на пороге — шумное дёргание следственной очереди и статуса канареечных пар | §4: полоса гистерезиса 50‰; смена класса требует пробить порог в сторону смены |

Синхронно: FPP v0.3 (§8.3 — протокольная привязка, §9.1/§10.2 — данные, Приложение B.2), реестр меток FGP-CRYPTO §1.1 (`device/tag`).

### B.2. Ревью 0.1 → 0.2 (bucket-профиль и отчёт панели)

Контекст: v0.1 нормировал квантование «5 уровней на метрику» и роль NS-D1 («класс определяет вес девайс-сигналов») декларативно, без нормативных границ, правил весов и формы выдачи для следственной панели — слой доведён до реализации (`fpp-core::profile`, `fpp-contracts`).

| # | С. | Дыра в v0.1 | Закрытие в v0.2 |
| --- | --- | --- | --- |
| S9 | В | Единый вес девайс-наблюдений позволял ферме без аттестации **самозаверять невиновность**: свежий software-ключ на аккаунт → `Exclusive`/`Stable` с весом серверного факта | §2 NS-D1: асимметричное правило — инкриминирующее наблюдение (коллизия, текучка) всегда A; экскульпирующее стоит столько, сколько стоит чеканка девайс-идентичности (hw → A, software → B, unattested → C) |
| S10 | С | «Расхождение C с A — самостоятельный сигнал» (§1) не имело нормативной метрики — неисполнимо и непроверяемо | §4.1 `reportConsistency`: max дистанция bucket'ов по метрикам, наблюдаемым обеими сторонами; пороги в Приложении A; зафиксировано вектором |
| S11 | С | Форма выдачи NS панели не была нормирована: панель могла запросить (и получить) больше, чем нужно, — ползучее расширение доступа против V-08 | §4.1 реестр переменных отчёта исчерпывающий; сборка — только из хранимого по §3; идентификатор субъекта в отчёте отсутствует; `None` нейтрален нормативно |
| S12 | С | Скор не раскрывал, на каких классах доказательности он стоит: «Natural из самоотчётов» и «Natural из наблюдений» выглядели одинаково | §4.1 `evidenceShares` (доли веса A/B/C) + полный список строк `signals[]` — скор исчерпывающе объясним |

Синхронно: §2 (NS-D1 правила весов), §3 (границы/репрезентативные точки), §4.1 (реестр переменных), §7 (модуль `profile`, порядок внедрения), Приложение A (9 новых строк параметров); vector `personhood-naturalness-v1.json` расширен секцией `panel`.

### B.3. Ревью 0.2 → 0.3 (эпоха, отчёт эпохи, wire-коды)

Контекст: v0.2 нормировал ротацию девайс-тега «с эпохой», но `epoch_id` оставался непрозрачным 16-байтовым входом, форма клиентского отчёта не существовала, а wire-коды части реестров не были зафиксированы — слой доведён до клиентской поверхности (`fpp-core::epoch`, `NaturalnessEpochReport`, `@flora/client-core/personhood`).

| # | С. | Дыра в v0.2 | Закрытие в v0.3 |
| --- | --- | --- | --- |
| S13 | В | `epoch_id` без канонической деривации: клиент и сервер могли разойтись в тегах (сигнал NS-D2 тихо умирает), а теги разных инсталляций — коллидировать | §2 NS-D2 каноническая эпоха: `index = ⌊(unix − genesis)/len⌋`, `epoch_id = LE64(genesis) ‖ LE64(index)`; genesis в id разводит инсталляции по построению; секция `epoch` вектора |
| S14 | В | Форма самоотчёта не нормирована: перестановки/дубли строк — скрытый канал, а клиент мог «самоотчитаться» девайс-метриками NS-D2/D3 с весом наблюдения | §3.1 контракт `NaturalnessEpochReport`: только темпоральные метрики, канонический порядок кодов, дубли/беспорядок → отказ целиком (`temporal_from_report`); негативный вектор |
| S15 | В | Класс аттестации NS-D1 мог попасть в самоотчёт — ферма декларировала бы `hardware_backed` бесплатно | §3.1: NS-D1 в отчёте отсутствует по построению — класс присваивает Verification по верифицированной регистрации девайс-ключа |
| S16 | С | Wire-коды были только у части реестров; `ReportConsistencyClass` начинался с 0 — «не задано» неотличимо от `consistent` при межъязыковой сериализации | §7: коды у всех 10 реестров, правило «0 зарезервирован» в реестрах классов, порядковые шкалы сохраняют 0; неизвестный код — отказ; секция `registries` + негативный вектор |
| S17 | С | Калибровочные кривые принимались без валидации формы: пустая кривая и неубывающие `x` молча давали «какой-то» результат `eval` | §7 `piecewise::validate` (`empty`, `y_above_permille`, `non_increasing_x`); негативный вектор `curveRejects` |

Синхронно: §2 (каноническая эпоха), §3.1 (контракт отчёта), §7 (модуль `epoch`, клиент TS, негативный вектор), Приложение A (2 строки); vector v1 расширен секциями `registries`/`epoch`/`epochReport`, добавлен `personhood-naturalness-negative-v1.json`; клиентское зеркало `@flora/client-core/personhood` с consumer-тестом.
