[package]
name = "flora-verification-contracts"
description = "Контракты модуля Verification: DTO и trait-порты, без бизнес-логики"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[lints]
workspace = true

# Правило §2.3: contracts-crate — только flora-shared и внешние crates.
[dependencies]
uuid = { workspace = true }
